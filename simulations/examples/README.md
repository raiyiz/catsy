# Canonical simulations

These modules are reproducible simulation recipes used by CI and the static Simulation Explorer.

They are deliberately separate from `src/catsy`: Catsy provides the reusable physics library; this
package provides runnable experiments and their configuration.
